
torch==2.7.1+cu128
torchvision==0.22.1+cu128
PySide6-Essentials==6.9.1
opencv-python-headless==4.10.0.84
matplotlib==3.10.9
pillow==12.3.0
numpy==2.2.6
scipy==1.15.3
pandas==2.3.3


lmdb==2.3.0
nvidia-ml-py==13.610.43

# rfdetr(含 train extra：lightning/torchmetrics/pycocotools/roboflow 等)
rfdetr[train]==1.9.2
transformers==5.15.0
supervision==0.30.0
pydantic==2.13.4
faster-coco-eval==1.7.2
pycocotools==2.0.11
roboflow==1.4.0
rf100vl==1.1.2
pytorch-lightning==2.6.5
torchmetrics==1.9.0
lightning-utilities==0.15.3
pyDeprecate==0.9.0
tqdm==4.70.0
requests==2.34.2

# 异常检测(AD). anomalib 本体 + 它 install_requires 里本发行版还没有的那些包,
# 版本锁自 rf-detr 开发环境(实测 PatchCore/CFA/STFPM/FastFlow/ReverseDistillation
# 五个算法都能建库/训练/评估).
#   * anomalib 只认统一包 lightning, 本文件上面的 pytorch-lightning==2.6.5 正好是
#     lightning==2.6.6 配套的那一版, 两个不能单独升
#   * 它要的 opencv 是 headless 版, 与本文件一致, 不会像 ultralytics 那样带进 GUI 版
#   * 骨干权重不在安装包里: timm 一律从 HuggingFace 取, 首次训练要联网.
#     离线机器需预置缓存, 路径 %USERPROFILE%\.cache\huggingface\hub\
#     models--timm--wide_resnet50_2.racm_in1k\snapshots\<sha>\model.safetensors (~263MB)
anomalib==2.6.2
lightning==2.6.6
timm==1.0.29
kornia==0.8.2
kornia_rs==0.1.14
einops==0.8.2
FrEIA==0.2
scikit-image==0.25.2
scikit-learn==1.7.2
joblib==1.5.3
threadpoolctl==3.6.0
imageio==2.37.4
lazy-loader==0.6
networkx==3.4.2
tifffile==2025.5.10
imagecodecs==2025.3.30
jsonargparse==4.46.0
docstring-parser==0.18.0
typeshed-client==2.13.0
omegaconf==2.3.1
antlr4-python3-runtime==4.9.3
platformdirs==4.11.2
rich==15.0.0
rich-argparse==1.8.0
markdown-it-py==4.2.0
mdurl==0.1.2
Pygments==2.20.0

# ultralytics(CNN/YOLO 架构)的三个传递依赖: ultralytics 本体在
# requirements-nodeps.txt 里以 --no-deps 装, 缺的这几个必须在这边钉住
ultralytics-thop==2.1.6
polars==1.44.2
cloudpickle==3.1.2

# 导出 ONNX. 三条路径各自要的包不同, 少任何一条那条导出就不可用:
#   分类   torch.onnx.export            -> onnx
#   YOLO   ultralytics export(simplify) -> onnx + onnxruntime + onnxslim
#   rf-detr 自家 export(含 simplify)    -> onnx + onnx_graphsurgeon + polygraphy + onnxsim
# ultralytics 遇到缺包会自己 pip install(不锁版本, 离线机器上还会直接失败),
# 所以这里显式钉住, 让它永远走"已满足"那条路
onnx==1.23.0
onnxruntime==1.23.2
onnxslim==0.1.96
onnxsim==0.7.3
onnx_graphsurgeon==0.6.1
polygraphy==0.53.4
